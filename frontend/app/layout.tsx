import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Manim Animator - Natural Language to Mathematical Animations",
  description:
    "Transform natural language descriptions into beautiful mathematical animations using Manim. Create educational content with ease.",
  keywords: "manim, mathematics, animation, education, visualization, natural language",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
